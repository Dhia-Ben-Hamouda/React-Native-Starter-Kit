import { colors, fonts, gutters, layout, backgrounds, borders } from "../theme";

export default function useTheme() {
    return {
        colors,
        fonts,
        gutters,
        layout,
        backgrounds,
        borders
    }
}